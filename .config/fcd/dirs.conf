# Fuzzy-cd directory list — SINGLE SOURCE OF TRUTH.
# One directory per line. Used by both the Ctrl+F kitty hotkey and the `fcd` command.
# Lines starting with # and blank lines are ignored. ~ and $HOME both work.
~/projects
~/jault/2026fall
~/.config
~/.config/nvim
~/zmk-config
~/scripts
~/Downloads
