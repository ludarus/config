# sioyek user keybinds. only overrides/additions go here; everything else is
# inherited from /etc/sioyek/keys.config.
# syntax:  command_name    keybinding

# open the command palette (default is ':', this adds Ctrl+P)
command_palette <C-p>
